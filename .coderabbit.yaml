reviews:
  profile: "CHILL"
  request_changes_workflow: false
  # Tells CodeRabbit to automatically review PRs targeting the 'test' branch
  target_branches:
    - "test"
    - "main" # You can keep this here if you also want PRs to main reviewed